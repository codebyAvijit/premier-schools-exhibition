import "./SearchBar.css";

const SearchBar = () => {
  return (
    <div className="search-bar">
      <input type="text" placeholder="Search for latest blogs..." />
    </div>
  );
};

export default SearchBar;
